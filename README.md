# 🌐 CIDR to Subnet Mask & IP Range Converter

**Завдання №14:** Реалізація конвертера CIDR-адреси у маску підмережі та повний діапазон IP-адрес.

---

## 📝 Опис завдання
Програма приймає рядок у форматі **CIDR** (наприклад, `192.168.1.0/24`) і обчислює:
* **Subnet Mask** — маску підмережі у десятковому форматі.
* **Network Address** — першу адресу мережі.
* **Broadcast Address** — останню адресу мережі.
* **IP Range** — повний простір адрес, доступний у межах даного префікса.

---

## ⚙️ Як це працює (Математика)

Для обчислення використовуються побітові операції над 32-бітними числами:

1.  **Маска:** Створюється шляхом зсуву `0xFFFFFFFF` вліво на `(32 - prefix)`.
    * *Приклад:* `/24` 👉 `11111111.11111111.11111111.00000000` (**255.255.255.0**)
2.  **Діапазон:**
    * `Start = IP & Mask` (Побітове **AND**)
    * `End = Start | ~Mask` (Побітове **OR** з інвертованою маскою)

📸 Скріншоти роботи програми
Нижче представлено інтерфейс та результати виконання розрахунків:

[!TIP] Кейс 1: Обробка стандартної мережі класу C.

[!IMPORTANT] Кейс 2: Робота з кастомними префіксами та перевірка коректності діапазону.

